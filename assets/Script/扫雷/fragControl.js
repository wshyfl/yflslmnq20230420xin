
cc.Class({
    extends: cc.Component,

    properties: {

    },

    // LIFE-CYCLE CALLBACKS:

    // onLoad () {},

    start() {
        // cc.director.on("dd1", (pos) => {
        //     if (pos == this.node.parent.x) {
        //         AD.audioMng.playdd1Music();
        //     }
        // }, this);
        // cc.director.on("dd2", (pos) => {
        //     if (pos == this.node.parent.x) {
        //         AD.audioMng.playdd2Music();
        //     }
        // }, this);
        // cc.director.on("dd3", (pos) => {
        //     if (pos == this.node.parent.x) {
        //         AD.audioMng.playdd3Music();
        //     }
        // }, this);
        // cc.director.on("dd1Stop", (pos) => {
        //     if (pos == this.node.parent.x) {
        //         AD.audioMng.stopdd1Music();
        //     }
        // }, this);
        // cc.director.on("dd2Stop", (pos) => {
        //     if (pos == this.node.parent.x) {
        //         AD.audioMng.stopdd2Music();
        //     }
        // }, this);
        // cc.director.on("dd3Stop", (pos) => {
        //     if (pos == this.node.parent.x) {
        //         AD.audioMng.stopdd3Music();
        //     }
        // }, this);
    },

    // update (dt) {},
});
